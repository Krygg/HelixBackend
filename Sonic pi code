set :bpm, 128


live_loop :foo do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo1 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet1"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo2 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet2"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo3 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet3"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo4 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet4"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo5 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet5"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo6 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet6"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo7 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet7"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo8 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet8"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo9 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet9"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo10 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet10"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo11 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet11"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo12 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet12"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo13 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet13"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo14 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet14"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end

live_loop :foo15 do
  use_real_time
  s,a, b, c, d = sync "/osc/trigger/prophet15"
  synth s, note: a, cutoff: b, sustain: c, attack: d
end
